class Circle:

    def __init__(self, radius, color):
        self.radius = radius
        self.color = color #digamos que queremos que todos los circulos inicien con un color en especifico, para ello en lugar de indicar el argumento 'color'
        #solamente indicamos el atributo -> ejemplo 'self.color = 'Blue'


class Rectangle:

    def __init__(self, length, width):
        self.length = length
    
        